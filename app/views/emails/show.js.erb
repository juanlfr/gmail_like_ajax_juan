
var selectedEmail = document.getElementById("<%=@email.id%>");

selectedEmail.addEventListener("click", function (){
    selectedEmail.style.fontWeight = "bold"
});


selectedEmail.nextElementSibling.innerHTML = "<%= j render 'show_email', email: @email %>"


